{
  "name": "commitmate",
  "displayName": "CommitMate",
  "icon": "images/icon.png",
  "description": "VSCode extension that generates commit messages for staged Git files",
  "version": "0.0.8",
  "engines": {
    "vscode": "^1.91.1"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onView:scm",
    "onLanguage:plaintext"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "extension.generateCommitMessage",
        "title": "Generate Commit Message (CommitMate)"
      },
      {
        "command": "commitmate.openOpenAiKeyPage",
        "title": "Get your OpenAI API key"
      }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "extension.generateCommitMessage",
          "when": "scmProvider == git",
          "group": "navigation"
        }
      ],
      "scm/input": [
        {
          "command": "extension.generateCommitMessage",
          "when": "scmProvider == git",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "type": "object",
      "title": "CommitMate Settings",
      "properties": {
        "commitmate.openAiApiKey": {
          "type": "string",
          "default": "",
          "description": "Your OpenAI API key for generating commit messages.",
          "markdownDescription": "Enter your OpenAI API key. Click [Get your API key](command:commitmate.openOpenAiKeyPage).",
          "scope": "window"
        },
        "commitmate.openAiModel": {
          "type": "string",
          "default": "gpt-5.4-nano",
          "enum": [
            "gpt-5.4",
            "gpt-5.4-mini",
            "gpt-5.4-nano",
            "custom"
          ],
          "description": "The model used by the OpenAI API",
          "markdownDescription": "Choose a GPT-5.4 series model, or select `custom` to use your own model name.",
          "scope": "window"
        },
        "commitmate.openAiCustomModel": {
          "type": "string",
          "default": "",
          "description": "Custom model name (used only when the model is set to custom).",
          "markdownDescription": "Custom model name used only when `commitmate.openAiModel` is set to `custom`.",
          "scope": "window"
        },
        "commitmate.openAiUrl": {
          "type": "string",
          "default": "https://api.openai.com/v1/responses",
          "description": "The OpenAI Responses API URL"
        },
        "commitmate.openAiTemperatureEnabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable sending a temperature value to the OpenAI API.",
          "markdownDescription": "When enabled, CommitMate sends a `temperature` value to the OpenAI API. Some models do **not** support temperature and will return an error if it is sent.",
          "scope": "window"
        },
        "commitmate.openAiTemperature": {
          "type": "number",
          "default": 0.4,
          "minimum": 0,
          "maximum": 2,
          "description": "Controls randomness vs. determinism in responses.",
          "markdownDescription": "Controls randomness vs. determinism in responses. Lower values (e.g., **0.2**) are more focused and consistent; higher values (e.g., **1.0**) are more creative and varied. **Model support:** support varies by model. If the selected model rejects `temperature`, disable this option to avoid 400 errors.",
          "scope": "window"
        },
        "commitmate.openAiReasoningEffortEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable sending reasoning effort to the OpenAI API.",
          "markdownDescription": "When enabled, CommitMate sends a `reasoning_effort` value to the OpenAI API. Disable this for custom models or if you prefer to rely on the model default.",
          "scope": "window"
        },
        "commitmate.openAiReasoningEffort": {
          "type": "string",
          "default": "none",
          "enum": [
            "none",
            "low",
            "medium",
            "high",
            "xhigh"
          ],
          "description": "Controls how much reasoning the model performs before responding.",
          "markdownDescription": "Controls how much reasoning the model performs before responding. Lower values are faster; higher values are more thorough. Supported for the built-in GPT-5.4 models are `none`, `low`, `medium`, `high`, and `xhigh`. For `custom` models, the value is sent as-is.",
          "scope": "window"
        },
        "commitmate.openAiReasoningSummary": {
          "type": "string",
          "default": "null",
          "enum": [
            "null",
            "none",
            "auto",
            "concise",
            "detailed"
          ],
          "description": "Controls whether the model returns a reasoning summary.",
          "markdownDescription": "Controls whether the model returns a reasoning summary. `null` means the field is omitted. Use `none` to explicitly disable summaries, or `auto` / `concise` / `detailed` to request one. **Support varies by model.**",
          "scope": "window"
        },
        "commitmate.openAiVerbosityEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable sending verbosity to the OpenAI API.",
          "markdownDescription": "When enabled, CommitMate sends a `verbosity` value to the OpenAI API. Disable this for custom models or if you prefer to rely on the model default.",
          "scope": "window"
        },
        "commitmate.openAiVerbosity": {
          "type": "string",
          "default": "low",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "description": "Controls how detailed the model's response should be.",
          "markdownDescription": "Controls how detailed the model's response should be. Lower values are more concise; higher values are more verbose. **Support varies by model.** If unsupported, the API may return an error.",
          "scope": "window"
        },
        "commitmate.requestPrefix": {
          "type": "string",
          "default": "Here is source code diff:",
          "description": "The prefix to be added to the GPT request message"
        },
        "commitmate.requestSuffix": {
          "type": "string",
          "default": "Generate a short or medium length commit message for these changes. Don't hallucinate, don't skip any changes, use past tense. Make sure to use past tense like word 'add' should be 'added' etc.",
          "description": "The suffix to be added to the GPT request message"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test",
    "clean": "rimraf out"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.7",
    "@types/node": "20.x",
    "@types/vscode": "^1.91.1",
    "@typescript-eslint/eslint-plugin": "^8.3.0",
    "@typescript-eslint/parser": "^8.3.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^9.9.1",
    "moxios": "^0.4.0",
    "rimraf": "^6.0.1",
    "sinon": "^19.0.2",
    "ts-sinon": "^2.0.2",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "@types/moxios": "^0.4.17",
    "axios": "^1.7.7"
  },
  "publisher": "nerexis",
  "repository": {
    "type": "git",
    "url": "https://github.com/Nerexis/commit-mate"
  }
}
